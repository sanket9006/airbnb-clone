/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['1000logos.net', 'picsum.photos', 'links.papareact.com'],
  },
};

module.exports = nextConfig;
